\* Broken instance: OrderDependentJoin = TRUE (a "last write wins" scalar
\* accumulator instead of the real set-then-Union join). TLC must find a
\* Monotonicity (and Soundness/OrderIndependence) violation among the 256
\* initial configurations; see MCBoxJoinOrder.tla's header.
SPECIFICATION Spec
CONSTANTS
    Scopes <- MCScopes
    Root <- MCRoot
    Captures <- MCCaptures
    WriteSites <- MCWriteSites
    ScopeOf <- MCScopeOf
    ConcreteTypes <- MCConcreteTypes
    TransitiveDiscovery <- MCTransitiveDiscovery
    OrderDependentJoin <- MCOrderDependentJoin
INVARIANT TypeOK
INVARIANT Soundness
INVARIANT Completeness
INVARIANT OrderIndependence
INVARIANT Monotonicity
PROPERTY Terminates
CHECK_DEADLOCK TRUE
