\* Broken instance: DropBoundscheckPhiStore = TRUE -- the pre-8424acd3
\* always-taken boundscheck arm, which realized its forward edge (a br, or an
\* implicit fallthrough) without running set_phi_locals_for_edge!. The real
\* bug: `@inbounds isvalid(SubString(" ab,c ", 2, 5), 1)` returned 0 (native
\* 1) -- the target phi kept its default, and no control claim noticed.
\* EXHAUSTIVE over the same N=4 class as MCStackifier.cfg: TLC must find a CFG
\* with such an edge and report EveryEdgeStoresPhis violated with pc = "Done"
\* (no reject in between: a silent wrong value, not a compile-time failure).
CONSTANTS
    N = 4
    SkipCrossingNormalization = FALSE
    UseSpanCarving = FALSE
    DropBoundscheckPhiStore = TRUE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    WellScopedBr
    Balanced
    EveryEdgeRealized
    EveryEdgeStoresPhis

PROPERTIES
    RejectNeverEmits

CHECK_DEADLOCK TRUE
