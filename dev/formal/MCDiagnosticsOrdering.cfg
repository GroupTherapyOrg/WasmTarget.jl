\* Correct instance: Phase 6.2's classify-before-emit ordering is in effect
\* for every call site (ClassifyAtBoundaryDef = all TRUE, ClassifyAfterEmit
\* off). InternalError must stay unreachable.
CONSTANTS
    Stmts <- StmtsDef
    Kind <- KindDef
    CallerHint <- CallerHintDef
    ProvenDead <- ProvenDeadDef
    ClassifyAtBoundary <- ClassifyAtBoundaryDef
    AllowTrapWithoutProof = FALSE
    ClassifyAfterEmit = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    NoSilentValue
    TrapNeedsProof
    InternalNeverTerminal
    LedgerComplete

PROPERTIES
    NoStuckUnsupported
