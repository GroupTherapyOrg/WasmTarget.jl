\* Broken instance: AllowTrapWithoutProof = TRUE -- the pre-guard behavior
\* where a Default-hint statement resolves non-fatal without the
\* stmt_is_proven_unreachable proof. s4 (unsupported_method, Default,
\* ProvenDead=FALSE -- a LIVE statement) then resolves ProvenDeadTrap, which
\* TrapNeedsProof forbids. TLC must report a violation.
CONSTANTS
    Stmts <- StmtsDef
    Kind <- KindDef
    CallerHint <- CallerHintDef
    ProvenDead <- ProvenDeadDef
    ClassifyAtBoundary <- ClassifyAtBoundaryDef
    AllowTrapWithoutProof = TRUE
    ClassifyAfterEmit = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    NoSilentValue
    TrapNeedsProof
    InternalNeverTerminal
    LedgerComplete

PROPERTIES
    NoStuckUnsupported
