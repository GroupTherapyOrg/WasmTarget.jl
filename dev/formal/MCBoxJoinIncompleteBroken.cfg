\* Broken instance: TransitiveDiscovery = FALSE (today's real one-hop
\* _f3_capturing_closure_bodies). TLC must find a Soundness/Completeness
\* violation among the 256 initial configurations (the Level2-grandchild-
\* write case; see MCBoxJoinIncomplete.tla's header).
SPECIFICATION Spec
CONSTANTS
    Scopes <- MCScopes
    Root <- MCRoot
    Captures <- MCCaptures
    WriteSites <- MCWriteSites
    ScopeOf <- MCScopeOf
    ConcreteTypes <- MCConcreteTypes
    TransitiveDiscovery <- MCTransitiveDiscovery
    OrderDependentJoin <- MCOrderDependentJoin
INVARIANT TypeOK
INVARIANT Soundness
INVARIANT Completeness
INVARIANT OrderIndependence
INVARIANT Monotonicity
PROPERTY Terminates
CHECK_DEADLOCK TRUE
