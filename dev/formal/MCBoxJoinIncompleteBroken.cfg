\* Broken instance: TransitiveDiscovery = FALSE (the PRE-FIX one-hop
\* _f3_capturing_closure_bodies; today's real code is transitive, see
\* MCBoxJoin.cfg). TLC must find a Soundness/Completeness violation among
\* the 256 initial configurations (the Level2-grandchild-write case; see
\* MCBoxJoinIncomplete.tla's header) -- this instance is kept as the
\* regression baseline TLC must continue to reject.
SPECIFICATION Spec
CONSTANTS
    Scopes <- MCScopes
    Root <- MCRoot
    Captures <- MCCaptures
    WriteSites <- MCWriteSites
    ScopeOf <- MCScopeOf
    ConcreteTypes <- MCConcreteTypes
    TransitiveDiscovery <- MCTransitiveDiscovery
    OrderDependentJoin <- MCOrderDependentJoin
INVARIANT TypeOK
INVARIANT Soundness
INVARIANT Completeness
INVARIANT OrderIndependence
INVARIANT Monotonicity
PROPERTY Terminates
CHECK_DEADLOCK TRUE
