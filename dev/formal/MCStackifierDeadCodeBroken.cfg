\* Broken instance: UseSpanCarving = TRUE -- the pre-b9f4d229 dead-code
\* mechanism (text span between an always-taken boundscheck jump and its
\* target, not reachability). TLC must find a CFG in this N=5 class where a
\* truly-reachable block's edges are silently dropped: EveryEdgeRealized
\* violated with pc = "Done" (no reject), matching that the real bug
\* (reduce(max, 1:n) on Julia 1.13 returning 0) was a silent wrong value,
\* not a compile-time failure.
CONSTANTS
    N = 5
    SkipCrossingNormalization = FALSE
    UseSpanCarving = TRUE

SPECIFICATION WitnessSpec

INVARIANTS
    TypeOK
    WellScopedBr
    Balanced
    EveryEdgeRealized

PROPERTIES
    RejectNeverEmits

CHECK_DEADLOCK TRUE
