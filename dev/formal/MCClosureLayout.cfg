\* Correct instance: HashOrderCapture=FALSE, AllowArityDrift=FALSE -- the
\* real algorithm exactly as it stands in structs.jl/closures.jl/types.jl
\* today. All five claims must hold across every arrival interleaving.
SPECIFICATION Spec
CONSTANTS
    Types <- MCTypes
    Arities <- MCArities
    FieldClasses <- MCFieldClasses
    DeclaredFields <- MCDeclaredFields
    DeclaredArity <- MCDeclaredArity
    MaxArrivals = 2
    HashOrderCapture = FALSE
    AllowArityDrift = FALSE

INVARIANTS
    TypeOK
    CapturedFieldsAreDeclaredOrder
    NoStructIdCollision
    NoGlobalIdCollision
    OneVtableStructPerArity
    WellFormedVtable

CHECK_DEADLOCK TRUE
