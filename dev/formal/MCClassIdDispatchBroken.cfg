\* Broken variant: dfs! visits a node's children in an ARBITRARY order instead of
\* sorted by name (the real `sort!(kids, by=T->string(T))` at types.jl:498 lost or
\* misapplied). TLC must report Determinism violated: two sibling subtrees swap
\* their id ranges depending on visitation order, which is then process-dependent.
SPECIFICATION Spec
CONSTANTS
    N = 6
    UnsortedIteration = TRUE
    NoCascadeReject = FALSE
    NoSpanGuard = FALSE
    Selectors <- MCSelectors
    Arity <- MCArity
    MethodSpace <- MCMethodSpace
INVARIANTS
    TypeOK
    RangesNest
    RangeIsa
    Determinism
CHECK_DEADLOCK FALSE
