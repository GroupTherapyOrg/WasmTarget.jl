\* Correct instance: both regression flags off. All invariants and the
\* liveness property must hold.
CONSTANTS
    Stmts <- StmtsDef
    Kind <- KindDef
    CallerHint <- CallerHintDef
    ProvenDead <- ProvenDeadDef
    ClassifyAtBoundary <- ClassifyAtBoundaryDef
    AllowTrapWithoutProof = FALSE
    ClassifyAfterEmit = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    NoSilentValue
    TrapNeedsProof
    InternalNeverTerminal
    LedgerComplete

PROPERTIES
    NoStuckUnsupported
