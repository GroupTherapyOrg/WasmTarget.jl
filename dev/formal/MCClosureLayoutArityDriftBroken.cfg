\* Broken instance: AllowArityDrift=TRUE -- ensure_closure_vtable! is
\* revisited for an already-seen closure type with a DIFFERENT arity than
\* its first arrival (lifting the "T-keyed: the wrap looks up by type
\* alone" assumption at closures.jl:39). TLC drives t1 (declared arity 1)
\* through a second EnsureVtable arrival at arity 2 (or t2 similarly at a
\* different arity): the cache-hit path returns lastAnnotated =
\* vtStructOf[2] while shapeAtCreation is still frozen at vtStructOf[1] --
\* WellFormedVtable is violated with no reject in between, reproducing the
\* literal closures.jl:40-44 / 153-154 recompute-on-cache-hit code path.
SPECIFICATION Spec
CONSTANTS
    Types <- MCTypes
    Arities <- MCArities
    FieldClasses <- MCFieldClasses
    DeclaredFields <- MCDeclaredFields
    DeclaredArity <- MCDeclaredArity
    MaxArrivals = 2
    HashOrderCapture = FALSE
    AllowArityDrift = TRUE

INVARIANTS
    TypeOK
    CapturedFieldsAreDeclaredOrder
    NoStructIdCollision
    NoGlobalIdCollision
    OneVtableStructPerArity
    WellFormedVtable

CHECK_DEADLOCK TRUE
