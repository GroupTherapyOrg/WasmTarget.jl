\* Broken: a MemoryRef re-reads the Array's current :ref instead of its snapshot. RefSnapshot must fail.
CONSTANTS
    Vals = {1, 2}
    MaxCap = 3
    MaxMems = 4
    MaxSteps = 4
    ReallocOnPopFirst = FALSE
    IgnoreRequestedLen = FALSE
    ReemitRef = TRUE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    InBounds
    Contents
    JuliaOffset
    RefSnapshot
