\* Broken instance: HashOrderNumbering = TRUE -- `add_global_ref!`'s
\* append-and-return-next-index (instructions.jl:709) is replaced by drawing
\* a newly-discovered class's slot from whatever id is left over, in NO
\* particular order (the L112 bug class -- test/parity_ratchet.jl:1426 --
\* lifted from `registry.structs` to `registry.constant_globals`). Within the
\* exhaustive N=3/2-class sweep, TLC finds a reachable "Done" state where an
\* eager occurrence's assigned id does not equal its class's discovery-order
\* rank -- a DeterministicNumbering violation.
CONSTANTS
    N = 3
    EqIds <- MCEqIds
    InternMutableKind = FALSE
    SkipChildEagerCheck = FALSE
    FabricateUndefFields = FALSE
    HashOrderNumbering = TRUE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Canonicalization
    MutableNeverAliases
    NoPartialIntern
    NoFabrication
    DeterministicNumbering

PROPERTIES
    RejectHalts

CHECK_DEADLOCK TRUE
