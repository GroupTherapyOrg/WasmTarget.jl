\* Broken instance: InternMutableKind = TRUE -- the funnel's `!ismutabletype(T)`
\* gate (types.jl:254) is dropped, so a "Mut"-kind occurrence becomes
\* eager-eligible. Within the exhaustive N=3/2-class sweep, TLC finds two
\* distinct "Mut" occurrences sharing an eqclass that both become "eager"
\* under the bug and dedupe to the SAME global -- a MutableNeverAliases
\* violation (the mutation-aliasing/=== breakage the real gate prevents).
CONSTANTS
    N = 3
    EqIds <- MCEqIds
    InternMutableKind = TRUE
    SkipChildEagerCheck = FALSE
    FabricateUndefFields = FALSE
    HashOrderNumbering = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Canonicalization
    MutableNeverAliases
    NoPartialIntern
    NoFabrication
    DeterministicNumbering

PROPERTIES
    RejectHalts

CHECK_DEADLOCK TRUE
