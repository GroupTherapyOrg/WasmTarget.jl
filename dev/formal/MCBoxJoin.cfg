\* Correct instance: TransitiveDiscovery = TRUE (the target, fully-recursive
\* discovery shape), OrderDependentJoin = FALSE (the real set-join). All
\* four claims hold EXHAUSTIVELY over the whole 256-initial-configuration
\* class and every fold order (~1-2s with default workers -- tiny state
\* space, see MCBoxJoin.tla's header for the bound).
SPECIFICATION Spec
CONSTANTS
    Scopes <- MCScopes
    Root <- MCRoot
    Captures <- MCCaptures
    WriteSites <- MCWriteSites
    ScopeOf <- MCScopeOf
    ConcreteTypes <- MCConcreteTypes
    TransitiveDiscovery <- MCTransitiveDiscovery
    OrderDependentJoin <- MCOrderDependentJoin
INVARIANT TypeOK
INVARIANT Soundness
INVARIANT Completeness
INVARIANT OrderIndependence
INVARIANT Monotonicity
PROPERTY Terminates
CHECK_DEADLOCK TRUE
