\* Broken variant: the pre-fix selector call -- rows packed without reserving the
\* span, no classId guard, no wrapper slot check. TLC must report MissingMethodTraps
\* violated: a MethodError receiver tuple runs another row.
SPECIFICATION Spec
CONSTANTS
    N = 4
    MaxLate = 0
    UnsortedIteration = FALSE
    NoCascadeReject = FALSE
    NoSpanGuard = TRUE
    ExtrasNeedRange = FALSE
    Selectors <- MCSelectors
    Arity <- MCArity
    MethodSpace <- MCMethodSpace
INVARIANTS
    TypeOK
    MissingMethodTraps
CHECK_DEADLOCK FALSE
