---
id: 9d411ad8ae24
status: fixed
category: strict_reject
kind: compiler_crash
construct: "COMPILER StackOverflow (crash, not a strict reject): `ntuple(i -> f(i), 16)` with a closure — explicit unrolling compiles fine (WASMMAKIE C-005)"
location: "WASMMAKIE geometry (mat4_mul)"
fn_name: repro
arg_types: "(Int64,)"
first_seen: wasmmakie-w003
---

# Gap `9d411ad8ae24` — COMPILER StackOverflow (crash, not a strict reject): `ntuple(i -> f(i), 16)` with a closure — explicit unrolling compiles fine (WASMMAKIE C-005)

**Category:** `strict_reject` &nbsp;•&nbsp; **Kind:** `compiler_crash` &nbsp;•&nbsp; **Location:** `WASMMAKIE geometry (mat4_mul)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
repro(x::Int64) = (t = ntuple(i -> Float64(i) * 2.0 + Float64(x), 16); Int64(round(t[16])))
bytes = try
    WasmTarget.compile(repro, (Int64,))
catch e
    error("WasmTarget gap: compiler exception $(typeof(e)) compiling ntuple(f, 16) closure")
end
length(bytes) > 8 || error("WasmTarget gap: empty module")
```

## Diagnostic
```
compiling ntuple(i -> ..., 16) overflows the compiler stack
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
