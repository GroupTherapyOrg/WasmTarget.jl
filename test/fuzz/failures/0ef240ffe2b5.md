---
id: 0ef240ffe2b5
status: open
category: compile_error
kind: compile_error
construct: "compile_error: `hypot(Inf, sinh(x))` :: Float64"
location: "test/fuzz (generated)"
fn_name: repro
arg_types: "(Float64,)"
first_seen: sweep-5-d3
---

# Gap `0ef240ffe2b5` — compile_error: `hypot(Inf, sinh(x))` :: Float64

**Category:** `compile_error` &nbsp;•&nbsp; **Kind:** `compile_error` &nbsp;•&nbsp; **Location:** `test/fuzz (generated)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
repro(x::Float64) = hypot(Inf, sinh(x))
WasmTarget.compile(repro, (Float64,))   # raises while the value-stub gap is present
```

## Diagnostic
```
WasmValidationError: wasm-tools rejected the emitted compiled module
error: func 1 failed to validate

Caused by:
    0: type mismatch: expected f64 but nothing on stack (at offset 0x2d3)
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
**Root: `sinh`/`cosh`/`tanh` value-stub (hyperbolic feature gap).** Not a hypot
bug — the `hypot(Float64,Float64)` overlay already returns `Inf` correctly when
either operand is infinite. The validation error "expected f64 but nothing on
stack" is the **second** operand `sinh(x)` producing no value: `sinh`/`cosh`/`tanh`
have no overlay and aren't natively codegen-able, so they emit a value-stub
(nothing on the stack) → the surrounding `hypot` call then has a missing f64
operand → module fails to validate.

Fix (Part 2): add Float64 overlays. `cosh(x) = (eˣ+e⁻ˣ)/2` is exact (no
cancellation). `sinh` needs a Taylor branch for |x| ≲ 0.35 (else catastrophic
cancellation in `eˣ-e⁻ˣ` blows past the differential's `rtol=1e-9` in the band
`[1e-12, 1e-7]`; below `1e-12` the `atol` saves it). `tanh = sinh/cosh` with a
`|x|>20 ⇒ ±1` guard (avoids `Inf/Inf` for large x). The generator also lists
`expm1/log1p/sinpi/cospi/deg2rad/rad2deg` — same value-stub class, same batch.
