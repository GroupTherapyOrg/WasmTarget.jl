---
id: 8558c726082d
status: open
category: validation_fail
kind: validation_fail
construct: "compile_closure_body dependency pass: failed dep bodies were SKIPPED after indices were reserved — every later function shifted one slot, cross-calls hit wrong targets (validation: not enough arguments on the stack). FIXED with typed unreachable placeholders; residual: closure-path call-typing still mismatches (call[0] expected i64, found i32) and many WasmMakie deps fail to compile in this path while compile_multi handles them (WASMMAKIE E-003)"
location: "WASMMAKIE Therapy island pipeline (compile_closure_body)"
fn_name: repro
arg_types: "(Int64,)"
first_seen: wasmmakie-e003
---

# Gap `8558c726082d` — compile_closure_body dependency pass: failed dep bodies were SKIPPED after indices were reserved — every later function shifted one slot, cross-calls hit wrong targets (validation: not enough arguments on the stack). FIXED with typed unreachable placeholders; residual: closure-path call-typing still mismatches (call[0] expected i64, found i32) and many WasmMakie deps fail to compile in this path while compile_multi handles them (WASMMAKIE E-003)

**Category:** `validation_fail` &nbsp;•&nbsp; **Kind:** `validation_fail` &nbsp;•&nbsp; **Location:** `WASMMAKIE Therapy island pipeline (compile_closure_body)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
# Repro requires the Therapy island pipeline: an @island effect building a
# WasmMakie Figure (lines! + render!) compiled via compile_closure_body —
# see Therapy.jl docs dashboard (WASMMAKIE E-003) and /tmp/e003min in the
# session notes. compile_multi over the same figure code validates and runs
# (WasmMakie W-002..W-005 gates). Self-verifying repro pending a minimal
# closure-path harness in this suite.
error("WasmTarget gap: closure-path repro harness not yet automated — see E-003 notes")
```

## Diagnostic
```
index skew fixed (placeholder); residual call-typing i64/i32 mismatch at function #108 in the minimal island
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
