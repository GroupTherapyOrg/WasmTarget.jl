---
id: f4b51570b855
status: fixed
category: compile_error
kind: compile_error
construct: "compile_error: `push!(sort(reverse([0, 0, -9223372036854775808])), get(Dict(0 => 0, 0 => 0), length([Int32(0), Int32(0), Int32(0)]), length(Dict(0 => \"\", 0 => \"abc\"))))` :: Vector{Int64}→Vector{Int64}"
location: "test/fuzz (natural)"
fn_name: repro
arg_types: "(Vector{Int64},)"
first_seen: sweep-nat-2
---

# Gap `f4b51570b855` — compile_error: `push!(sort(reverse([0, 0, -9223372036854775808])), get(Dict(0 => 0, 0 => 0), length([Int32(0), Int32(0), Int32(0)]), length(Dict(0 => "", 0 => "abc"))))` :: Vector{Int64}→Vector{Int64}

**Category:** `compile_error` &nbsp;•&nbsp; **Kind:** `compile_error` &nbsp;•&nbsp; **Location:** `test/fuzz (natural)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
include(joinpath("test", "fuzz", "structpool.jl")); using .FuzzStructPool
FuzzStructPool.build_pool!()
repro(v::Vector{Int64}) = push!(sort(reverse([0, 0, -9223372036854775808])), get(Dict(0 => 0, 0 => 0), length([Int32(0), Int32(0), Int32(0)]), length(Dict(0 => "", 0 => "abc"))))
WasmTarget.compile(repro, (Vector{Int64},))   # raises while the gap is present
```

## Diagnostic
```
WasmValidationError: wasm-tools rejected the emitted compiled module
error: func 1 failed to validate

Caused by:
    0: type mismatch: expected i32, found i64 (at offset 0x27b7)
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
