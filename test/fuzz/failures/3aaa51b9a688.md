---
id: 3aaa51b9a688
status: open
category: validation_fail
kind: validation_fail
construct: "ANY Matrix{Float64} construction fails V8 validation: struct.new[2] expected (ref null 18), found (ref N) — even bare Matrix{Float64}(undef,2,2) with one store/load; literals, reshape, minimum, size all affected (WASMMAKIE W-005)"
location: "WASMMAKIE heatmap/image kernels"
fn_name: repro
arg_types: "(Int64,)"
first_seen: wasmmakie-w005
---

# Gap `3aaa51b9a688` — ANY Matrix{Float64} construction fails V8 validation: struct.new[2] expected (ref null 18), found (ref N) — even bare Matrix{Float64}(undef,2,2) with one store/load; literals, reshape, minimum, size all affected (WASMMAKIE W-005)

**Category:** `validation_fail` &nbsp;•&nbsp; **Kind:** `validation_fail` &nbsp;•&nbsp; **Location:** `WASMMAKIE heatmap/image kernels`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
include(joinpath("test", "fuzz", "harness.jl")); using .FuzzHarness
function repro(x::Int64)
    m = Matrix{Float64}(undef, 2, 2)
    m[1, 1] = 7.0 + Float64(x)
    return Int64(round(m[1, 1]))
end

_nat = repro(Int64(0))
_r = FuzzHarness.compile_and_run_vec(repro, (Int64,), [(Int64(0),)])
_ok = _r isa Vector && _r[1][1] === :ok && _r[1][2] == _nat
_ok || error("WasmTarget gap: native=$_nat wasm=$_r (validation failure / trap)")
```

## Diagnostic
```
V8: CompileError: struct.new[2] expected type (ref null 18), found struct.new of type (ref 32) — Matrix (Array{T,2}) memoryref struct field type mismatch in codegen
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
