---
id: e817213d1890
status: open
category: compile_error
kind: compile_error
construct: "compile_error: `reinterpret(Int32, x::Float32)` / `reinterpret(UInt32, c::Char)` :: bitcast from 32-bit non-integer"
location: "test/fuzz/bridge (M1 round-trip)"
fn_name: repro
arg_types: "(Float32,)"
first_seen: bridge-m1
---

# Gap `e817213d1890` — compile_error: `reinterpret(Int32, x::Float32)` / `reinterpret(UInt32, c::Char)` :: bitcast from 32-bit non-integer

**Category:** `compile_error` &nbsp;•&nbsp; **Kind:** `compile_error` &nbsp;•&nbsp; **Location:** `test/fuzz/bridge (M1 round-trip)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
repro(x::Float32)::Int32 = reinterpret(Int32, x)
WasmTarget.compile(repro, (Float32,); validate=true)   # WasmValidationError while the gap is present
```

## Diagnostic
```
wasm-tools rejects the module: "type mismatch: expected i64, found anyref" — the 32-bit reinterpret path emits a boxed/anyref value where an i32 bitcast (i32.reinterpret_f32) is expected. Same failure shape for reinterpret(UInt32, ::Char). 64-bit reinterpret(Int64, ::Float64) is CORRECT. Workarounds in FuzzBridge: f32 bits via exact Float64 widening; Char via codepoint().
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
