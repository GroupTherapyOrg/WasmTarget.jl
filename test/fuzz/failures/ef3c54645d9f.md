---
id: ef3c54645d9f
status: open
category: runtime_trap
kind: runtime_trap
construct: "runtime_trap: `get(Dict(Int32(0) => 0, sum([Int32(0), Int32(0), Int32(0)]) => 0), Int32(0), 0)` :: Int64 (abstract-Signed-key Dict — was a validation error, now compiles + runtime-traps)"
location: "test/fuzz (generated)"
fn_name: repro
arg_types: "(Int64,)"
first_seen: sweep-stmt-3
---

# Gap `ef3c54645d9f` — runtime_trap: `get(Dict(Int32(0) => 0, sum([Int32(0), Int32(0), Int32(0)]) => 0), Int32(0), 0)` :: Int64

**Category:** `runtime_trap` &nbsp;•&nbsp; **Kind:** `runtime_trap` &nbsp;•&nbsp; **Location:** `test/fuzz (generated)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.
(Strengthened 2026-06-22 from a compile-only check to a runtime differential check — the
overlay-aware concrete-eval fix removed the COMPILE/validation error, exposing that the
construct then traps at RUNTIME, so a compile-only reproducer false-closes it.)

```julia
using WasmTarget
include(joinpath("test", "fuzz", "harness.jl"));     using .FuzzHarness
include(joinpath("test", "fuzz", "bridge.jl"));      using .FuzzBridge
include(joinpath("test", "fuzz", "bridge_args.jl")); using .FuzzBridgeArgs
include(joinpath("test", "fuzz", "structpool.jl"));  using .FuzzStructPool
FuzzStructPool.build_pool!()
repro(x::Int64) = get(Dict(Int32(0) => 0, sum([Int32(0), Int32(0), Int32(0)]) => 0), Int32(0), 0)
_x = 0
_nat = try (:ok, repro(deepcopy(_x))); catch e; (:throw, e); end
_rt = Core.Compiler.widenconst(Base.code_typed(repro, (Int64,))[1][2])
_rt === Union{} && (_rt = Int64)
_rr = FuzzBridgeArgs.bridge_run_args(repro, (Int64,), [(deepcopy(_x),)]; rettype = _rt)
_rr isa Vector || error("bridge could not run reproducer: " * string(_rr))
_res = _rr[1]
_ok = _nat[1] === :throw ? (_res[1] === :trap) :
    (_res[1] === :ok && FuzzBridge.tree_matches(FuzzBridge.descriptor(_rt)[1], _nat[2], _res[2]))
_ok || error("WasmTarget gap @ x=$_x : native=$(_nat[1] === :throw ? :throw : _nat[2]) wasm=$(_res[1]) $(_res[2])")
```

## Diagnostic
```
WAS (pre-fix, validation): type mismatch: expected (ref null $type), found (ref null $type) (at offset 0x722)
NOW (post overlay-aware concrete-eval fix): compiles + validates clean, then at x=0 native=0 wasm=trap (unreachable)
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis

**2026-06-22 (wt-soundness-loop-4) — VALIDATION error fixed, but RE-OPENED as
runtime_trap (NOT a verified-correct close).** The overlay-aware concrete-eval fix
([`3fd2f07bfc5c`](3fd2f07bfc5c.md)) removed this construct's `(ref null $type)`
VALIDATION failure: the abstract-key/Type-level promotion now folds at inference time
instead of leaking `dynamic` dispatch on Type values that WT stubbed to `unreachable`.
But once it compiles+validates, it TRAPS at runtime (native `get(...)=0`, wasm
`unreachable`) — it had merged into the abstract-Signed-key Dict runtime-trap cluster
(`05b2b084ef65` et al., still open). `verify` initially false-closed it because the
old reproducer only checked `compile`; the reproducer above is now a RUNTIME
differential check, and the gap stays **open** (runtime_trap) until the Dict cluster's
real runtime fix lands. So this fix's verified-correct tally is the 4 `cor` gaps, not 5.

**Re-record artifact warning (P3 bugsmash):** depth-4 sweeps keep
shrinking unrelated Dict compositions into this gap's structural
bucket, sometimes with HOMOGENEOUS Int64 keys (`Dict(0 => 0,
sum([0,0,0]) => 0)` etc.). Those forms compile and run fine in a clean
process — the shrink sessions had already hit the hvcat-family
StackOverflow ("program state may be corrupted"), so their re-recorded
diagnostics (e.g. a bare `StackOverflowError`) are bogus. One such
re-record was reverted 2026-06-11; keep the original
WasmValidationError diagnostic. The genuine limitation remains the
hetero-key promotion documented below/README.


_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_

## Triage — KNOWN LIMITATION (P3 bugsmash, 2026-06-12)

Heterogeneous-key Dict literals (`Dict(Int32(0) => 0, sum(Int32[...]) => 0)`
— the computed key is Int64, so the pairs are Pair{Int32,Int64} +
Pair{Int64,Int64}) route through Base.dict_with_eltype/_compute_eltype,
whose inferred result is the UNPARAMETERIZED `Dict` — every downstream
operation then needs runtime type dispatch, which WasmGC codegen does not
support (same root as heterogeneous-union vararg tuple reads, see
284d3e7059cd triage). Fixing requires either a dynamic-dispatch runtime or
inference-level monomorphization of the promoted Dict type. Documented in
README Known Limitations; left open-triaged intentionally.
**Workaround:** promote keys explicitly so all pairs share one type:
`Dict(Int64(k1) => v1, k2_int64 => v2)`.
