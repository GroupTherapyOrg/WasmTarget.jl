---
id: ef3c54645d9f
status: out_of_subset
category: compile_error
kind: compile_error
construct: "compile_error: `get(Dict(Int32(0) => 0, sum([Int32(0), Int32(0), Int32(0)]) => 0), Int32(0), 0)` :: Int64"
location: "test/fuzz (generated)"
fn_name: repro
arg_types: "(Int64,)"
first_seen: sweep-stmt-3
---

# Gap `ef3c54645d9f` — compile_error: `get(Dict(Int32(0) => 0, sum([Int32(0), Int32(0), Int32(0)]) => 0), Int32(0), 0)` :: Int64

**Category:** `compile_error` &nbsp;•&nbsp; **Kind:** `compile_error` &nbsp;•&nbsp; **Location:** `test/fuzz (generated)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
include(joinpath("test", "fuzz", "structpool.jl")); using .FuzzStructPool
FuzzStructPool.build_pool!()
repro(x::Int64) = get(Dict(Int32(0) => 0, sum([Int32(0), Int32(0), Int32(0)]) => 0), Int32(0), 0)
WasmTarget.compile(repro, (Int64,))   # raises while the gap is present
```

## Diagnostic
```
WasmValidationError: wasm-tools rejected the emitted compiled module
error: func 1 failed to validate

Caused by:
    0: type mismatch: expected (ref null $type), found (ref null $type) (at offset 0x722)
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis

**Re-record artifact warning (P3 bugsmash):** depth-4 sweeps keep
shrinking unrelated Dict compositions into this gap's structural
bucket, sometimes with HOMOGENEOUS Int64 keys (`Dict(0 => 0,
sum([0,0,0]) => 0)` etc.). Those forms compile and run fine in a clean
process — the shrink sessions had already hit the hvcat-family
StackOverflow ("program state may be corrupted"), so their re-recorded
diagnostics (e.g. a bare `StackOverflowError`) are bogus. One such
re-record was reverted 2026-06-11; keep the original
WasmValidationError diagnostic. The genuine limitation remains the
hetero-key promotion documented below/README.


_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_

## Triage — KNOWN LIMITATION (P3 bugsmash, 2026-06-12)

Heterogeneous-key Dict literals (`Dict(Int32(0) => 0, sum(Int32[...]) => 0)`
— the computed key is Int64, so the pairs are Pair{Int32,Int64} +
Pair{Int64,Int64}) route through Base.dict_with_eltype/_compute_eltype,
whose inferred result is the UNPARAMETERIZED `Dict` — every downstream
operation then needs runtime type dispatch, which WasmGC codegen does not
support (same root as heterogeneous-union vararg tuple reads, see
284d3e7059cd triage). Fixing requires either a dynamic-dispatch runtime or
inference-level monomorphization of the promoted Dict type. Documented in
README Known Limitations; left open-triaged intentionally.
**Workaround:** promote keys explicitly so all pairs share one type:
`Dict(Int64(k1) => v1, k2_int64 => v2)`.
