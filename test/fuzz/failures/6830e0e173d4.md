---
id: 6830e0e173d4
status: open
category: compile_error
kind: compile_error
construct: "compile_error: `gcd(length(Dict(0 => 0.0, x => 0.0)), length([0, 0, x]))` — ArgumentError(\"===: too few arguments\") — CONTEXT-SENSITIVE; also poisons subsequent compiles in the session (IOError: stream closed)"
location: "test/fuzz (P2 triage)"
fn_name: repro
arg_types: "(Int64,)"
first_seen: p2-triage
---

# Gap `6830e0e173d4` — compile_error: `gcd(length(Dict(0 => 0.0, x => 0.0)), length([0, 0, x]))` — ArgumentError("===: too few arguments") — CONTEXT-SENSITIVE; also poisons subsequent compiles in the session (IOError: stream closed)

**Category:** `compile_error` &nbsp;•&nbsp; **Kind:** `compile_error` &nbsp;•&nbsp; **Location:** `test/fuzz (P2 triage)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
# Context-sensitive: compiles CLEAN in reduced repros (named fn, lambda,
# lambda+harness) but fails reproducibly inside the probe batch:
#     julia --project=test/fuzz test/fuzz/failures/repro_eq_args_probe.jl <repo-root>
# Expected once fixed: probe 1 reports ✓ and no subsequent IOError poisoning.
error("manual gap — run the probe file above; auto-verify not applicable yet")
```

## Diagnostic
```
WasmTarget.compile emits a `===` dispatch guard with one argument in some compilation-order-dependent state (likely a cache/registry carrying state across compiles — same expr compiles clean in a fresh context). SECOND symptom: after this ArgumentError, subsequent unrelated compiles in the same session fail with IOError, so a single failed compile poisons the session — find the shared mutable state that is not cleaned up on the error path.
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
