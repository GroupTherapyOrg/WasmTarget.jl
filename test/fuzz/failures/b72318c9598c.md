---
id: b72318c9598c
status: open
category: runtime_trap
kind: runtime_trap
construct: "runtime_trap: `isempty(Dict(\"\" => 0, string(0.0) => 0))` :: Vector{Int64}→Bool"
location: "test/fuzz (natural)"
fn_name: repro
arg_types: "(Vector{Int64},)"
first_seen: nat-5-d3
---

# Gap `b72318c9598c` — runtime_trap: `isempty(Dict("" => 0, string(0.0) => 0))` :: Vector{Int64}→Bool

**Category:** `runtime_trap` &nbsp;•&nbsp; **Kind:** `runtime_trap` &nbsp;•&nbsp; **Location:** `test/fuzz (natural)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
include(joinpath("test", "fuzz", "harness.jl")); using .FuzzHarness
repro(v::Vector{Int64}) = isempty(Dict("" => 0, string(0.0) => 0))
function _m(a, b)
    if a isa AbstractVector && b isa AbstractVector
        length(a) == length(b) || return false
        return all(_m(x, y) for (x, y) in zip(a, b))
    elseif a isa AbstractFloat || b isa AbstractFloat
        return (isnan(a) && isnan(b)) || (isinf(a) && isinf(b) && sign(a) == sign(b)) ||
               a == b || isapprox(float(a), float(b); rtol = 1e-9, atol = 1e-12)
    end
    return a == b
end
_v = Int64[]
_nat = try (true, repro(deepcopy(_v))) catch; (false, nothing) end   # deepcopy: don't let mutation alias wasm's input
_r = FuzzHarness.compile_and_run_vec(repro, (Vector{Int64},), [(deepcopy(_v),)])[1]
_ok = _nat[1] ? (_r[1] === :ok && _m(_nat[2], _r[2])) : (_r[1] === :trap)
_ok || error("WasmTarget gap @ v=$_v : native=$(_nat[1] ? _nat[2] : :throw) wasm=$_r")
```

## Diagnostic
```
at v=Int64[]: native=false  wasm=trap
```

## Diagnostic update (P2-batch9, 2026-06-10)
string(0.0) itself now works (string(::Float64) overlay; gaps 5741ab865252,
2e5dfd5c93c2 closed). THIS reproducer still fails because its natural
Vector{Int64} signature pulls the bridge accessor set into the module and the
Ryu writeshortest kernel then emits UNBALANCED CONTROL FRAMES (wasm-tools:
"control frames remain at end of function body", func $writeshortest) — the
same large-function stackifier defect that mis-routes control flow into dead
`unreachable` markers on the general digit path (string(1.5) traps). ROOT
CAUSE IS NOW THE STACKIFIER ON ~2000-stmt FUNCTIONS, module-context-sensitive.
Also involved: silent Int128 op stubs (calls.jl PURE-908 sites) on the
mulShift64 path.

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
**Roots: Float→string (Ryu) + Dict-of-String construction.** `string(0.0)` needs
`Base.Ryu`; it's a `Dict{String,Int}` key here, so this is the same Ryu root as
`2e5dfd5c93c2` layered on Dict construction. `isempty` over the malformed Dict
then traps. Needs Ryu first (Part 2).
