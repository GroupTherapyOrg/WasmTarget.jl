---
id: b9d9c2d60d86
status: open
category: wrong_value
kind: wrong_value
construct: "wrong_value: `Char(88)` :: Char — Char internal repr incoherent (literal=reinterpret-bits, codepoint=identity, Char(::Int)=identity/0)"
location: "test/fuzz (bridge/M3)"
fn_name: repro
arg_types: "(Char,)"
first_seen: m3-char
---

# Gap `b9d9c2d60d86` — wrong_value: `Char(88)` :: Char — Char internal repr incoherent (literal=reinterpret-bits, codepoint=identity, Char(::Int)=identity/0)

**Category:** `wrong_value` &nbsp;•&nbsp; **Kind:** `wrong_value` &nbsp;•&nbsp; **Location:** `test/fuzz (bridge/M3)`

## Reproducer
Contract: this snippet **throws while the gap is present** and **runs cleanly once fixed**.
A follow-up loop fixes the compiler, then `verify_gaps!()` re-runs this to auto-close the gap.

```julia
using WasmTarget
include(joinpath("test", "fuzz", "harness.jl")); using .FuzzHarness
repro(x::Char)::Int64 = Int64(codepoint(Char(88)))
_r = FuzzHarness.compile_and_run(repro, (Char,), [(Int64(97),)])[1]
_r[1] === :ok && _r[2] == 88 || error("WasmTarget gap: codepoint(Char(88)) => $(_r) (expected 88)")
```

## Diagnostic
```
Native: Char literals/X store UTF-8-justified bits (reinterpret(UInt32, X) = 0x58000000) and codepoint decodes them to 0x58. Wasm: literals emit the justified bits (consistent) BUT codepoint returns them UNDECODED (1476395008) and Char(::Int64) does not encode (identity; observed 0 through the ctor path). Net: every Char-valued arg/return crosses the bridge corrupted — Char is transport-blocked for the differential until ONE of codepoint / Char(::Int) / 32-bit reinterpret (gap e817213d1890) is fixed. Char-typed INTERMEDIATES with Bool/Int results (isdigit, ==) remain testable.
```

## Work on this
```
julia --project=test/fuzz test/fuzz/run.jl verify
```

## Analysis
_(No analysis yet. Add root-cause notes below the `## Analysis` heading — they are PRESERVED across re-records.)_
