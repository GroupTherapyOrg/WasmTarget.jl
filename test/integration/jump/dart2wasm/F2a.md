# F2a dart2wasm mechanism audit

The structural oracle is `dart-lang/sdk` at
`898a1e4bbfbc472dc0a9505dc7d2e4c21d6f856e`. References below are file and
symbol names at that exact revision. Dart is an architectural comparison, not
the semantic oracle for Julia or MathOptInterface; the committed MOI
expectations and native Julia execution remain authoritative.

| Mechanism | Pinned dart2wasm source | WasmTarget reuse or Julia adaptation | Explicitly out of scope at F2a | Executable invariant and evidence |
| --- | --- | --- | --- | --- |
| Closed-world allocated-class discovery | `pkg/dart2wasm/lib/compile.dart:478-527` establishes entry points and runs type flow; `pkg/dart2wasm/lib/functions.dart` tracks allocated classes and newly reachable targets. | **Adapted.** WasmTarget closes over the concrete MOI set, index, carrier, and exception types reachable from each F2a entry point before assigning layouts. | Dart tree shaking, class IDs, call shapes, and RTI semantics are not Julia or MOI oracles. | Every case records its exact source-function contract and executes the complete authored input ledger in raw, `-Os`, and `-O3` modules. |
| Parametric runtime type identity | `pkg/dart2wasm/lib/class_info.dart:199-300,665-777,780-1050` builds class metadata and representation relationships; `pkg/dart2wasm/lib/types.dart:434-609,840-1016` represents and tests runtime types. | **Adapted to Julia.** F2a constructs the nine concrete `ConstraintIndex{VariableIndex,S}` families behind a union-typed barrier and dynamically recovers each family using Julia `isa`; it also witnesses the exact `VariableIndex` and constraint payloads. | Dart subtype rules, interface identities, and generic covariance do not certify Julia parametric invariance or multiple dispatch. | Six boundary payloads for each of nine families must produce the independently authored family/payload witness on native Julia and all retained Wasm variants. |
| Recursive object and field layouts | `pkg/dart2wasm/lib/class_info.dart:199-300,665-777` builds superclass-first concrete layouts and field representations. | **Adapted.** F2a uses the exact pinned MOI immutable set/index layouts and a passively constructed `VariablesContainer{Float64}` carrier. | This does not certify container mutation, validity, deletion, duplicate-bound, or `Parameter` lifecycle behavior. | Reconstruction must read the exact stored lower/upper carrier fields and return bit-level witnesses for every certified set family. |
| Numeric representation | `sdk/lib/_internal/wasm/common/boxed_double.dart:188-245,324-370` handles Dart double representation and operations. | **Julia-specific semantics.** No Dart numeric result is reused. F2a transports `Float64` values as exact `Int64` bit patterns and compares them with independently authored MOI expectations. | Dart equality, NaN canonicalization, boxing, and arithmetic semantics are not evidence for Julia. Signaling NaNs are not certified. | The bounded ledger covers signed zero, positive/negative subnormal and normal edges, finite extrema, infinities, two quiet-NaN sign/payload encodings, reversed bounds, and fractional `Semiinteger` endpoints. |
| Optimized/unoptimized differential | `pkg/dart2wasm/lib/compile.dart:159-183` declares dart2wasm's closed-world Binaryen feature/pass pipeline. | **Adapted.** WasmTarget retains one raw module plus independently derived `-Os` and `-O3` variants per case. | Matching Dart's exact pass sequence or output bytes is not required. Cross-host byte identity is not claimed. | Each retained module must pass pinned `wasm-tools` validation, remain within its budget, and independently reproduce the complete committed semantic ledger. |
| Correct-or-loud internal rejection | `pkg/dart2wasm/lib/translator.dart:494,1025-1035` initializes and exposes Dart/JS exception tags; `pkg/dart2wasm/lib/tags.dart:9-49` defines their module-level representation. Their behavior is not a Julia exception oracle. | **Narrow Julia evidence only.** Compiled F2a code invokes the exact pinned MOI private helpers, catches their expected `AssertionError` or `MethodError`, and returns an integer witness. | This does not certify uncaught exception transport across the JavaScript boundary, stack traces, exception identity, or general `throw`/`catch`. | Zero/composite/deleted/unknown masks and an unsupported set must reject with the exact bounded failure class; silent acceptance or another class fails promotion. |
| FFI, interface dispatch, maps, and solver behavior | dart2wasm contains dedicated interface-dispatch, map, FFI, and runtime machinery outside the cited F2a paths. | **Not reused at F2a.** These mechanisms begin only in milestones that execute their Julia counterparts. | Dictionaries, CleverDict, open-world dispatch, JuMP frontend behavior, bridges, native libraries, and solvers remain unclaimed. | A later milestone may not cite F2a as evidence for any omitted mechanism; it must add its own pinned audit and executable corpus. |

The F2a local evidence therefore exercises a bounded, closed `Float64` MOI
variable-bound vocabulary and passive storage/reconstruction path only. F2a
becomes certified only after its independent cross-platform, promotion,
rejection, and Snapshot browser gates pass. Snapshot browser delivery is
separate downstream evidence and cannot waive any compiler, semantic-oracle,
or promotion failure.
